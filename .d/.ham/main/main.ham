#!/bin/bash

if find ${REPO_PATH}/Repono -type f -size +45M -not ${REPO_PATH}/Repono/.git ; then
    plt_info "in ${REPO_PATH}/_repo/Repono/.d/.ham/main/main.ham : TRUE_EXEC : find ${REPO_PATH}/Repono -type f -size +45M : return 1"
    return 1
fi
return 0
